<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gxx.mapper.GirlMapper">

    <select id="findAll" resultType="com.gxx.Girl">
        SELECT * FROM girl s WHERE 1=1;
    </select>
    <select id="findAll1" resultType="com.gxx.Girl">
        SELECT * FROM girl s WHERE 1=1;
    </select>
    <select id="findAll11" resultType="map">
        SELECT * FROM girl s ;
    </select>
    <insert id="test1" parameterType="com.gxx.Girl">
        insert into
		girl(age,money,str)
		values(#{age},#{money},#{str})
    </insert>

</mapper>